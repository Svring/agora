# Prompts for the DM AI
# from langchain_core.prompts import PromptTemplate

# # Example prompt for processing player action
# PLAYER_ACTION_PROMPT = PromptTemplate.from_template("""
# You are the Dungeon Master for a game based on the following background: {story_background_summary}.
# The current game state is: {game_state_summary}.
# A player (ID: {player_id}) has performed the following action: {player_action}.
# Based on this, the DM profile ({dm_profile_name}), and relevant world/lore cards, describe the outcome.
# """)

# # Example prompt for narrative generation
# NARRATIVE_GENERATION_PROMPT = PromptTemplate.from_template("""
# You are the Dungeon Master for a game based on the following background: {story_background_summary}.
# The current game state is: {game_state_summary}.
# Based on the DM profile ({dm_profile_name}), and recent events, generate a short narrative update or description of the current scene.
# """)
